/* ── Supabase config ─────────────────────────────────────────
 * Paste your project URL + public anon key from:
 *   Supabase dashboard → Project Settings → API
 * The anon key is SAFE to expose — RLS policies enforce per-user access.
 * Leave as empty strings to disable login entirely (app falls back to
 * localStorage-only mode).
 * ───────────────────────────────────────────────────────────── */
window.SP_SUPABASE = {
  url:      '',  // e.g. 'https://abcdefghij.supabase.co'
  anonKey:  '',  // e.g. 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....'
};
